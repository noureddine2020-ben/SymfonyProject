{% extends 'base.html.twig' %}

{% block title 'Tous nos matériels' %}

{% block body %}

    <div class="jumbotron">
        <div class="container">
       {{ form_start(form) }}
            <div class="form-row align-items-end">
                <div class="col">
              {{ form_row(form.maxPrice) }}
                </div>
                <div class="col">
                    {{ form_row(form.typeMateriel) }}
                </div>
                <div class="col">
                    <div class="form-group">
                    <button class="btn btn-secondary"> <i class="fa fa-search"></i> Rechercher </button>
                </div>
                </div>
            </div>
            {{ form_end(form) }}

        </div>

    </div>
        <div class="container mt-4">
            <h1><FONT face="arial" color="#d2691e" size=""><b>Voir Tous Nos Matériels</b> <i class="fas fa-bell"></i></FONT></h1>
            <div class="row">
            {% for property in properties %}
               <div class="col-md-4">
              {% include 'pages/pagin.html.twig' %}
               </div>
            {% endfor %}
    </div>
<div class="pagination">
    {{ knp_pagination_render(properties) }}
</div>
{% endblock %}